{                                       //configure application for the data base in use by setting up the connection string to our data base
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": { //setting up conncetion strings
    "DefaultConnection": "server = DESKTOP-N8Q9KHF; Database = EmployeeRecordDb;Trusted_Connection = true; TrustServerCertificate=true"
  }
}
